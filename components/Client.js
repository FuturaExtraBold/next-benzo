import React from "react";

const Client = (props) => {
  return (
    <div className="client">
      <div className="client__placeholder"></div>
      {/* <img src={ `http://www.davidahays.com/cdn/img/clients/${ props.img }` } alt={ props.alt } /> */}
    </div>
  );
}

export default Client;
